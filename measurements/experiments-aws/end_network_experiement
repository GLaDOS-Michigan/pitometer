#!/bin/bash
# Ends a non-terminating net-aws experiment 

# Kill the local coordinator's process to trigger start_network_trial script to collect results
kill -9 $(ps -ux | grep "network-aws-main" | grep bash | xargs | cut -d' ' -f 2)

# # Sleep
# sleep 5

# # Kill
# ./kill_all_experiments